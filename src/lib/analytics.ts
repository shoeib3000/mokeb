// Firebase dependency removed

export async function trackSiteVisit() {
  // Analytics functionality is currently disabled
}

export async function incrementVisit(mokebId?: string, categoryId?: string) {
  // Analytics functionality is currently disabled
}
